#ifndef STRING_UTILS_H
#define STRING_UTILS_H

char* new_string(char*);
char* concat(char*, char*);
char* concat_char(char*, char);
char* concat_indent(char*, char*);
char* concat_comment(char*, char*);

#endif